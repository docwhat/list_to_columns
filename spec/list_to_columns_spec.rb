require 'spec_helper'

describe ListToColumns do
  it 'has a version number' do
    expect(ListToColumns::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
